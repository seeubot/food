# MongoDB Connection URI
# IMPORTANT: Replace the placeholder with your actual MongoDB connection string.
# Ensure there are NO spaces around the `=` sign.
MONGODB_URI="mongodb+srv://room:room@room.4vris.mongodb.net/?retryWrites=true&w=majority&appName=room"

# JSON Web Token Secret for Admin Authentication
# IMPORTANT: Generate a strong, random string for this in production.
# You can use a tool like 'node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"'
JWT_SECRET="b5e2caea986c5564491c17e3d9e0d4b4"

# Optional: Set NODE_ENV for different environments (e.g., development, production)
# NODE_ENV=development

