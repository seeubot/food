# --- Server Configuration ---
# Port for the Express server to listen on. Default is 8080.
PORT=8080

# --- MongoDB Configuration ---
# Your MongoDB Atlas connection URI.
# Replace this with your actual MongoDB connection string.
# Example: mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/?retryWrites=true&w=majority&appName=<app-name>
MONGODB_URI="mongodb+srv://room:room@room.4vris.mongodb.net/?retryWrites=true&w=majority&appName=room"

# --- WhatsApp Bot Configuration ---
# The WhatsApp number of the admin (without the '+' sign or any spaces).
# This number will receive notifications for new orders.
# Example: 918897350151
ADMIN_NUMBER="918897350151"

# --- Express Session Configuration ---
# A strong, secret key for signing the session ID cookie.
# IMPORTANT: Change this to a long, random string in production for security!
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="e94e38b695b378f26d764ab1d8f36c3e"

# --- Deployment Specific (Optional) ---
# If deploying to a platform like Koyeb, replace 'YOUR_KOYEB_URL' with your actual deployed URL.
# This is used for generating correct links in WhatsApp messages (e.g., menu URL).
# NODE_ENV=production
# YOUR_KOYEB_URL="https://jolly-phebe-seeutech-5259d95c.koyeb.app"

